---
description: React 组件模式和最佳实践
globs: **/*.tsx
alwaysApply: false
---

# React 组件模式

## 组件结构

- 使用函数式组件和 Hooks
- 使用 TypeScript 接口定义 Props 类型
- 将组件相关的样式文件放在同一目录

```typescript
// ✅ GOOD
interface ImageWrapperProps {
  url: string;
  selected: boolean;
  onImageLoad: (data: ImageType) => void;
  onSelectionChange: (url: string) => void;
}

function ImageWrapper({ url, selected, onImageLoad, onSelectionChange }: ImageWrapperProps) {
  // component logic
}
```

## Hooks 使用

- 使用 `useState` 管理本地状态
- 使用 `useRef` 存储可变值（如 interval ID）
- 使用 `useEffect` 处理副作用，记得清理资源
- 提取自定义 Hooks 用于可复用逻辑

```typescript
// ✅ GOOD
useEffect(() => {
  const intervalId = setInterval(() => {
    // do something
  }, 1000);
  
  return () => {
    clearInterval(intervalId);
  };
}, []);
```

## Chakra UI 使用

- 使用 Chakra UI 组件而非原生 HTML 元素
- 使用 `useToast` 显示通知消息
- 保持组件样式与 Chakra UI 主题一致

```typescript
// ✅ GOOD
import { Button, Stack, Box } from '@chakra-ui/react';
import { useToast } from '@chakra-ui/react';

const toast = useToast();
toast({
  title: '操作成功',
  status: 'success',
  isClosable: true,
});
```

## 性能优化

- 使用 `useMemo` 和 `useCallback` 优化性能（仅在必要时）
- 避免在渲染函数中创建新对象或数组
- 合理使用条件渲染
